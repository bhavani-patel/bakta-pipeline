nextflow.enable.dsl = 2

params {
    input_fasta = 'genome.fasta'
    outdir = 'results'
    bakta_db = '/path/to/bakta/db'
}

process {
    withName: bakta {
        container = 'oschwengers/bakta'  // Or use conda below
    }
}

docker.enabled = true
